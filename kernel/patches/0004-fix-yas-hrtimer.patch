diff --git a/drivers/sensors/yas_mag_kernel.c b/drivers/sensors/yas_mag_kernel.c
--- a/drivers/sensors/yas_mag_kernel.c
+++ b/drivers/sensors/yas_mag_kernel.c
@@ -481,7 +481,7 @@
 		data->mag.set_enable(1);
 		data->mag.set_delay(data->poll_delay);
 		hrtimer_start(&data->mag_timer, data->delay,
-						HRTIMER);
+						HRTIMER_MODE_REL);
 	} else {
 		data->mag.set_enable(0);
 	}
