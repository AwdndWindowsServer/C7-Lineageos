diff --git a/drivers/staging/android/alarm-dev.c b/drivers/staging/android/alarm-dev.c
--- a/drivers/staging/android/alarm-dev.c
+++ b/drivers/staging/android/alarm-dev.c
@@ -95,6 +95,13 @@
 		hrtimer_cancel(&alrm->u.hrt);
 }
 
+static void set_power_on_alarm(void)
+{
+	/* Not implemented on the msm8953 A6 kernel; provided so the
+	 * RTC_POWEROFF_WAKEUP call site compiles.
+	 */
+}
+
 static void alarm_clear(enum android_alarm_type alarm_type, struct timespec *ts)
 {
 	uint32_t alarm_type_mask = 1U << alarm_type;
